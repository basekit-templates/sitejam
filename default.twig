{% extends "layout.twig" %}

{% block content %}

<div class="bk-pt-default">

    <div class="content">

        <!-- HEADER !-->
        {% include '/includes/header.twig' %}

        <section class="feature page-title">
            <div class="wrapper">
                <h2>{{ page.title }}</h2>
            </div>
        </section>
        <div class="wrapper main"> 
            {{zone('main')|raw}}
        </div>
        <!-- FOOTER !-->
        {% include '/includes/footer.twig' %}
        
    <!-- CONTENT END !-->
    </div>

</div>

{% endblock %}
    